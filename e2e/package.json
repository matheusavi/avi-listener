{
  "name": "avilistener-e2e",
  "private": true,
  "type": "module",
  "description": "End-to-end tests for the AviListener dashboard.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
